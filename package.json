{
  "name": "inspeccion-baterias",
  "version": "1.1.0",
  "description": "PWA para inspección de baterías con sincronización en Google Sheets",
  "main": "index.html",
  "scripts": {
    "dev": "netlify dev",
    "build": "echo 'Static site - no build needed'",
    "deploy": "netlify deploy --prod"
  },
  "dependencies": {
    "node-fetch": "^3.3.0"
  },
  "devDependencies": {
    "netlify-cli": "^17.0.0"
  }
}
